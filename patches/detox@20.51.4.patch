diff --git a/src/server/DetoxServer.js b/src/server/DetoxServer.js
index c0231f5516cd85436da37bbe0bb1dba8ed27d15a..1cfe30a23723fc802ad79730a98182254502be96 100644
--- a/src/server/DetoxServer.js
+++ b/src/server/DetoxServer.js
@@ -107,6 +107,15 @@ class DetoxServer {
     }, timeoutValue);
 
     try {
+      // ws >=8 only invokes the close() callback once every client connection is
+      // closed; a lingering app connection otherwise keeps the server (and the node
+      // process) alive until the test job times out. force-terminate any remaining
+      // clients first so close() resolves immediately.
+      if (this._wss.clients) {
+        for (const client of this._wss.clients) {
+          client.terminate();
+        }
+      }
       this._wss.close(() => {
         clearTimeout(handle);
         this._closing.resolve();
