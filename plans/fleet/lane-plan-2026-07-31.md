# Fleet plan 2026-07-31 — four lanes after the reboot

Written by Lane E (fable, planner). Manager: a2461. Supersedes the six-lane
split in `plans/v3-fleet-lanes.md` for lane count and routing; every other rule
in that file (ownership discipline, tuple gate, hot-path rules, git rules)
still applies verbatim.

Gates for everyone, measured at `e43e37c917`: grammar 319/16 files (322 after
Lane V's validator), web 771 passed / 1 skipped / 1 todo / 55 files, native 411
passed / 7 expected fail / 11 skipped / 21 files, static-tests 104 passed /
2 skipped / 12 files, webpack 18 passed. Web is currently 770: the
`tailwindThemeColor.web.test.tsx` failure from the v6 border-color rename is
known and owned by CODEX-1 item 1.

## Cross-lane dependency map (read this first)

- CODEX-2's Tailwind-remainder items (public `StyleMode` removal, the
  `preprocessStyleModeProps` core tests, the `tailwind-merge` dependency drop)
  are BLOCKED on Lane E item 2, the protected-file descriptor cut. Lane E does
  it second, right after its orphan triage, precisely so this block is short.
- CODEX-2's DOM-compiler items (former Lane C) are BLOCKED on OPUS item 1
  landing the tag/attribute/event/native-backing/compatibility tables. The
  tables are the source of truth; no second table.
- Lane E item 3 (wiring the shared candidate-target validator) consumes the
  validator module CODEX-1 already landed in style-grammar (`d1e39772e5`); not
  blocked, the module exists.
- CODEX-1's final corpus re-run (item 4) is blocked on Lane E item 1 (the
  clause-free cutover unlocks the 1,435 waiting sites).
- Nobody is blocked on the review duty: Lane E reviews hot-path diffs as they
  arrive.

## USER DECISIONS — not scheduled, do not build

- **Button/explicit component-lowering descriptor**
  (`plans/decision-24-component-lowering.md`). Both designs are parked. This is
  an architecture commitment (a public compiler ABI per component) and is the
  user's call. No lane builds any part of it.
- **Codemod apply mode.** The codemod stays report-only until all three runtime
  gates close (clause-free — Lane E item 1; same-key styled/call-site clause
  merging — Lane E item 6; `containerName` reaching the host — Lane E item 7).
  When all three are green, flipping apply-mode on is a user sign-off, not a
  lane decision.
- **Native DOM ref API and `style()` multi-handle composition** (design items
  6/8). OPUS writes proposals into the handoff log; the user picks.

---

## Lane E — engine (fable, high) — this session

Exclusive files: the seven `@tamagui/web` runtime files + `propMapper.ts` per
the lane contract. Standing reviewer for all hot-path diffs from other lanes.

1. **Triage and land the orphaned clause-free cutover diff** (the five
   modified `helpers/*` files). The diff is the config-first cutover: the
   `hasClauses` short-circuit removed from `contributePrograms.ts`, the
   `indexOf(':')` gate removed from `getSplitStyles.tsx`, x/y moved to
   `defaultTokenCategories` as `space`, the grammarConfig workaround deleted.
   Verify the `baseValue` slot in `lowerAccumulatedPrograms.ts` has a producer
   or drop it; measure the parse-cost impact of clause-free strings entering
   the parse cache (hot-path rule: measured, never asserted).
   Accept: grammar/web/native gates green (web back to 771 only after CODEX-1
   item 1; until then 770 with exactly that one known failure), codemod corpus
   re-run shows the waiting-site count moving from 1,435 toward converted,
   committed with explicit pathspecs, logged in `plans/v3-handoff-log.md`.
2. **Protected-file Tailwind descriptor cut** (unblocks CODEX-2).
   `createComponent.tsx`: remove the global style-mode preprocessor, read the
   immutable descriptor from static config, one `preprocessProps` call at the
   hoisted location, unify `STYLE_FRONTEND_PREPROCESSED` with
   `STYLE_MODE_PREPROCESSED`. `getSplitStyles.tsx`: remove the embedded
   Tailwind candidate/cache/static-normalization code and the `tailwind-merge`
   import, dispatch through the descriptor, unknown-class path runs
   `flushForwardStylesToClasses()` + `shouldDoClasses = false` for
   descriptor-selected components. Accept: web+native+tailwind package suites
   green, message manager so CODEX-2 starts its unblocked items.
3. **Wire the shared candidate-target validator** from style-grammar into the
   resolver: an overloaded-family mismatch (`fontSize="red-500"`) becomes a
   diagnostic, never silent literal CSS. Accept: grammar suite (validator
   tests) + a web-runtime test for the diagnostic path.
4. **Remaining fallback-category conversion**: exotic transform parts
   (skews/3D), `borderBlock`/`borderInline`/`font`/`textDecoration` families,
   nested platform chains, base unconditional styles. New style-grammar
   modules follow the borderFamily model. Accept: no live caller of the legacy
   value pipeline for these categories; gates green.
5. **The engine contraction**: delete pseudo-object blocks + `getSubStyle`
   recursion, media-object sub-style path + importance ordering, `usedKeys`
   tiers, the specificity ladders / pseudo `!important` / `.cls.cls` doubling
   in `getCSSStylesAtomic`, `$theme-*`/`$platform-*`/`$group-*` key parsing.
   Then the two unifications (one emitter, one resolver). Accept: MEASURED
   `@tamagui/web` bundle-size and `getSplitStyles` branch-count reduction
   reported beside the bundle gate, parse-cost + render-loop benchmarks run,
   all gates green.
6. **Same-key styled/call-site clause merging** before `mergeComponentProps`
   discards the styled value (codemod apply gate 2).
7. **`containerName` reaching the host** (codemod apply gate 3).

## CODEX-1 (gpt-5.6-sol, high) — v6 config, codemod, transitions

Exclusive files: `code/core/config*/**`, `code/core/shorthands/**`,
`code/core/codemod-flat-values/**`, new transition modules under
`code/core/style-grammar/src/` (append-only in that package), and
`code/core/core-test/tailwindThemeColor.web.test.tsx` for item 1.

1. **Triage and land the Lane V orphans** (config `v6-base.ts` +
   `v6-theme-names.ts`, codemod `builtInNames.ts`/`grammar.ts`/tests,
   `tailwindThemeColor.web.test.tsx`, README). This carries three outstanding
   manager directives that must close together: (a) the `border-borderColor`
   regression — web gate back to 771, no test special-casing; (b) the full
   16-name consumer sweep (Tailwind candidates, theme templates, component
   defaults, docs examples) recorded in the handoff log; (c) the
   `backgroundActive` answer as designed — v6 defines neither
   `backgroundActive` nor `background-active`, codemod maps legacy uses to
   `background-press` with a migration note. Accept: web 771, codemod suite
   green, corpus counts re-run and logged, committed.
2. **Triage and land the transition orphans** (`transition.ts`,
   `transitionLegacy.ts`, `transitionNative.ts`, tests, index/package edits).
   Deliverables per the Lane V brief: the verified native capability matrix
   sourced from `plans/react-native-style-capabilities.md` (unsupported
   combinations are diagnostics, never silent approximation), and migration of
   the array and per-property preset object forms onto the
   shorthand/longhand model. Accept: grammar suite green at its new count,
   package builds, logged.
3. **Codemod rename-rule finalization**: the explicit 16-entry table routed
   through `builtInNames.ts`, report-only posture and waiting-site accounting
   untouched. Accept: full codemod suite + corpus run with site counts logged.
4. **Post-cutover corpus re-run** (blocked on Lane E item 1): re-run the
   default corpus, record how many of the 1,435 waiting sites now convert, and
   list what still blocks the rest (should be exactly Lane E items 6 and 7).
   Do NOT flip apply mode — user decision.

## CODEX-2 (gpt-5.6-sol, high) — compiler + Tailwind remainder

Exclusive files: `code/compiler/static/src/**` (except `dom*` until its own
item 6), `code/compiler/static-tests/tests/**`, `code/core/tailwind/**`,
`code/core/to-tailwind/**`, `code/compiler/vite-plugin/**` Tailwind seams,
`styleMode` callers outside Lane E files.

1. **Triage and land the animatedBy orphan** (`compilerHost.ts`,
   `e3-lowerer.web.test.ts`, `spec/extract-specs.tsx`, `webpack.test.tsx`).
   The approved semantics: `animatedBy` is a driver selector, not an animation
   source; erase/flatten only when no actual animation source is present.
   Accept: pinned output test + webpack DOM snapshot for the group selector
   and child hover class, static-tests and webpack suites green, tuple keeps
   `found` at 2,556 and `flattened` rises by exactly the recovered case(s),
   logged.
2. **Verify the recoverable/structurally-retained classification is complete**
   (partially landed in `4e237c9911`): the headline in the handoff log is the
   classified split with the raw tuple unchanged underneath, the structural
   set is pinned so a component silently gaining `isHOC` fails a test, and the
   log states plainly that the honest recoverable pool is single digits of
   2,556. Accept: metric run green, log updated. Then Lane S is done — no new
   lowering work; the Button descriptor is a user decision.
3. **Tailwind isolation remainder, unblocked part**: graph and type-entry
   isolation tests (prove `@tamagui/tailwind` never imports the core root or
   inline-style frontend, Tailwind components never instantiate regular
   inline-style types, nothing Tailwind-shaped reachable from plain core).
   Accept: resolver-graph tests green, module graphs recorded.
4. **Tailwind isolation remainder, blocked part** (waits on Lane E item 2):
   remove the public `StyleMode` setting from types, remove/replace the
   `preprocessStyleModeProps` core tests, drop the `tailwind-merge` dependency
   everywhere outside Lane E files. Accept: web/native/tailwind suites green,
   no `styleMode` in public types, `tailwind-merge` absent from the dependency
   graph.
5. **DOM compiler, former Lane C** (waits on OPUS item 1 tables): the seven
   Phase-3 items — member-expression recognition for imported `html.div` with
   import provenance following the existing extractor pattern; JSX /
   `jsx`/`jsxs` / `createElement` normalization proof; a web fixture rewritten
   to literal semantic tags; a native fixture rewritten to placeholder DOM
   primitives; compile-time literal text wrapping (on native a structural
   requirement — unwrapped text is a build failure, never a silent pass);
   one unsupported-prop and one invalid-nesting diagnostic; proof no RSD or
   StyleX code enters either bundle. New modules named `dom*`; hook edits in
   shared plugin files minimal and committed immediately. Accept: static-tests
   + webpack green with the new fixtures, tuple `found` unchanged.

## OPUS (opus-5, xhigh) — DOM contract runtime

Exclusive files: new `code/core/dom/**`, `code/core/web/src/dom/**` (new
directory), `tamagui/dom` + `@tamagui/core/dom` entries, generated strict DOM
prop interfaces and conformance tables.

1. **Triage and land the scaffold orphan** (`code/core/dom/`, untracked):
   package skeleton, `extract-rsd-snapshot.ts`, `rsd-snapshot.json`, and the
   five tables (tags, attributes, events, nativeBacking, compatibility).
   Validate the tables against the pinned RSD snapshot, finish them, commit.
   These are the source of truth for CODEX-2 item 5 — tell the manager the
   moment they land.
2. **Generate the explicit prop interfaces** from the tables.
3. **Minimum native semantic primitives**, honoring the native perf rules in
   the Lane D brief: no callback ref when none passed, optional behavior
   allocates nothing when unused, every generic-host-path cost measured with
   command/sample-size/variance recorded.
4. **Expose `html.*` from core**; the `@tamagui/tailwind` half is a routed
   request to CODEX-2, not an edit.
5. **`tamagui/dom` + `@tamagui/core/dom` entries** and the missing-compiler
   build failures (native without compiler = explicit build error; standalone
   dom is compile-only both platforms).
6. **`style()`** on the same style-definition grammar as `styled()`, one
   handle per call. Write the two open-design proposals (ref API, multi-handle
   composition) into the handoff log for the user — do not decide
   unilaterally.

Accept per item: gates green, RSD/StyleX absent from bundles, validation
matrix cases from the brief covered as they become expressible, status
appended to handoff log item 3.

## Backlog (route when a lane frees up, codex-first)

- Lint and editor tooling (Phase 5 item 6): ESLint rule, canonical formatting,
  language-service completions — all backed by style-grammar, never a second
  parser.
- Reactive safe-area on native (design item 12).
- Validation debt: font-face swap E2E, streaming SSR + code-splitting app
  fixture, WebKit program-block re-check.
