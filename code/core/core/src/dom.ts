/**
 * `@tamagui/core/dom` — standalone Tamagui DOM.
 *
 * `html.*`, `style()`, and on native the DOM primitives the compiler injects.
 * The entry is compile-only on both platforms; see `standalone.ts` in
 * `@tamagui/web` for what that means and why.
 */

export * from '@tamagui/web/dom'
