import { createAnimations } from '@tamagui/animations-css/extras'
import { animationPresets } from '@tamagui/animation-helpers'

export const animationsCSS = createAnimations(animationPresets)

export const animations = animationsCSS
