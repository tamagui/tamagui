// Runtime defaults are generated together from the pinned Tailwind source. Keeping this seam
// hand-written makes the generated artifact replaceable without leaking generator concerns into
// the public config composition.
export {
  tailwindColors,
  tailwindFontSize,
  tailwindLineHeight,
  tailwindRadius,
  tailwindSize,
  tailwindSource,
  tailwindSpace,
  tailwindZIndex,
} from './v6-tailwind-defaults.generated'
