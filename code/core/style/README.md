## style

Note: most of this logic moved into @tamagui/web

style is a superset of web, it just adds in some react-native props support.

This package was named `@tamagui/core` before v3. `@tamagui/core` still works as an
alias that re-exports this package.
