/**
 * `tamagui/dom` — standalone Tamagui DOM.
 *
 * The same entry as `@tamagui/style/dom`; `tamagui/dom` is the name to reach for
 * from an app, and `@tamagui/style/dom` is the lower-level alias.
 */
export * from '@tamagui/style/dom';
//# sourceMappingURL=dom.d.ts.map