/**
 * `tamagui/dom` — standalone Tamagui DOM.
 *
 * The same entry as `@tamagui/core/dom`; `tamagui/dom` is the name to reach for
 * from an app, and `@tamagui/core/dom` is the lower-level alias.
 */
export * from '@tamagui/core/dom';
//# sourceMappingURL=dom.d.ts.map