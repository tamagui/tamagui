import type { SkinManifest } from './registry-manifest'

export const manifest = {
  description:
    'v2-look Progress: theme background on the track and indicator + pill radius on the track, over the unstyled @tamagui/ui Progress behavior (clip, size-derived track height, and the indeterminate/complete/loading state machine). Static: Progress.Indicator.',
  categories: ['feedback'],
  tokens: ['$background'],
} satisfies SkinManifest
