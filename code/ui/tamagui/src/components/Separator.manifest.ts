import type { SkinManifest } from './registry-manifest'

export const manifest = {
  description:
    'v2-look Separator: theme line color over the unstyled @tamagui/ui Separator behavior (orientation + layout + the collapsed 1px rule, transparent by default). Supports the vertical variant.',
  categories: ['layout'],
  tokens: ['$backgroundFocus'],
} satisfies SkinManifest
