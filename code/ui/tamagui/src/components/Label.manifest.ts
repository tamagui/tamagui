import type { SkinManifest } from './registry-manifest'

export const manifest = {
  description:
    'v2-look Label: theme text color + press color feedback, over the unstyled @tamagui/ui Label behavior (label element, flex alignment, selection/cursor resets, the size-derived font mechanism, and the htmlFor/aria wiring).',
  categories: ['form'],
  tokens: ['$color', '$colorPress'],
} satisfies SkinManifest
