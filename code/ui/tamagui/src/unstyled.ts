// `tamagui/unstyled` — the unstyled, behavior-first primitives.
//
// Re-exports @tamagui/ui so consumers can opt into the unstyled surface without
// pulling the default v2-look skins that `tamagui`'s main entry layers on top.
export * from '@tamagui/ui'
