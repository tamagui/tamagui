{
  "name": "next15-plus-cli-optimize",
  "version": "2.0.0-rc.6-1770409768999",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:web": "vitest run",
    "test:unit": "vitest run",
    "test:clean": "git checkout packages/app/ && git clean -fd packages/app/"
  },
  "dependencies": {
    "@tamagui/cli": "workspace:*",
    "tamagui": "workspace:*"
  },
  "devDependencies": {
    "vitest": "4.0.4"
  }
}
