---
title: Context Menu
description: A Context Menu
name: context menu
component: ContextMenu
package: context-menu
---

# Context Menu

<Description>
  A menu component that is triggered by right click on web and long press on touch devices
  native
</Description>

<HeroContainer>
  <ContextMenuDemo />
</HeroContainer>

```tsx hero template=ContextMenu

```

<Highlights
  features={[
    'Full keyboard navigation.',
    'Support items, icons, images, checkbox, groups, and more ...',
    'Support submenu',
    'Suppport modal and non modal',
    'Support native menu on native',
    'Customize alignment, offsets and position',
    'Native ios and android icons support',
    'Support Preview on iOS',
  ]}
/>

## Installation

ContextMenu is already installed in `tamagui`, or you can install it independently:

```bash
yarn add @tamagui/context-menu
```

if you want to use native Menus then make sure to install these depedencies in you expo directory

```bash
yarn add @react-native-menu/menu
```

```bash
yarn add react-native-ios-context-menu
```

```bash
yarn add react-native-ios-context-menu
```

## Anatomy

Import all parts and piece them together.

```jsx
import { Accordion } from 'tamagui';

export default () => (
    <ContextMenu>
      <ContextMenu.Trigger asChild>
        <YStack>
          <Text>Right Click or longPress</Text>
        </YStack>
      </ContextMenu.Trigger>

      <ContextMenu.Portal zIndex={100}>
        <ContextMenu.Content>
          <ContextMenu.Item>
            <ContextMenu.ItemTitle>About Notes</ContextMenu.ItemTitle>
          </ContextMenu.Item>
          <ContextMenu.Item>
            <ContextMenu.ItemTitle>Settings</ContextMenu.ItemTitle>
          </ContextMenu.Item>
          <ContextMenu.Item>
            <ContextMenu.ItemTitle>
              <Text>Calender</Text>
            </ContextMenu.ItemTitle>
            <ContextMenu.ItemIcon>
              <Calendar color="gray" size="$1" />
            </ContextMenu.ItemIcon>
          </ContextMenu.Item>
          <ContextMenu.Separator />
          <ContextMenu.Sub>
            <ContextMenu.SubTrigger>
                <ContextMenu.ItemTitle>Actions</ContextMenu.ItemTitle>
            </ContextMenu.SubTrigger>
            <ContextMenu.Portal zIndex={200}>
              <ContextMenu.SubContent
              >
                <ContextMenu.Label fontSize={'$1'}>Note settings</ContextMenu.Label>
                <ContextMenu.Item onSelect={onSelect} key="create-note">
                  <ContextMenu.ItemTitle>Create note</ContextMenu.ItemTitle>
                </ContextMenu.Item>
                <ContextMenu.Item onSelect={onSelect} key="delete-all">
                  <ContextMenu.ItemTitle>Delete all notes</ContextMenu.ItemTitle>
                </ContextMenu.Item>
                <ContextMenu.Item onSelect={onSelect} key="sync-all">
                  <ContextMenu.ItemTitle>Sync notes</ContextMenu.ItemTitle>
                </ContextMenu.Item>
              </ContextMenu.SubContent>
            </ContextMenu.Portal>
          </ContextMenu.Sub>
        </ContextMenu.Content>
      </ContextMenu.Portal>
    </ContextMenu>
)
```

## API Reference

### Context Menu

Contains every component for the Context Menu.

<PropsTable
  data={[
    {
      name: 'children',
      type: 'React.ReactNode',
      required: true,
    },
    {
      name: 'placement',
      type: 'Placement',
      required: false,
      description: `'top' | 'right' | 'bottom' | 'left' | 'top-start' | 'top-end' | 'right-start' | 'right-end' | 'bottom-start' | 'bottom-end' | 'left-start' | 'left-end'`,
    },
    {
      name: 'open',
      type: 'boolean',
      required: false,
      description: ``,
    },
    {
      name: 'defaultOpen',
      type: 'boolean',
      required: false,
    },
    {
      name: 'onOpenChange',
      type: '(open: boolean) => void',
      required: false,
    },
    {
      name: 'onOpenWillChange',
      type: '(open: boolean) => void',
      required: false,
      platform: 'ios',
    },
    {
      name: 'modal',
      type: 'boolean',
      default: 'true',
      required: false,
      description: `Renders into root of app instead of inline.`,
    },
    {
      name: 'stayInFrame',
      type: 'ShiftProps | boolean',
      required: false,
      description: `Keeps the Popover inside the frame, see floating-ui shift().`,
    },
    {
      name: 'allowFlip',
      type: 'FlipProps | boolean',
      required: false,
      description: `Moves the Popover to other sides when space allows it, see floating-ui flip().`,
    },
    {
      name: 'offset',
      type: 'OffsetOptions',
      required: false,
      description: `Determines the distance the Popover appears from the target, see floating-ui offset().`,
    },
    {
      name: 'unstyled',
      required: false,
      type: `boolean`,
      description: `Removes all default Tamagui styles.`,
    },
  ]}
/>

### Portal

This is necessary for Context Menu

<PropsTable
  data={[
    {
      name: 'zIndex',
      required: false,
      type: 'number',
    },
    {
      name: 'children',
      type: 'React.ReactNode',
      required: true,
    },
  ]}
/>

### Trigger

ContextMenu will only trigger when user right click or long press inside Trigger area

<PropsTable
  data={[
    {
      name: 'action',
      required: false,
      type: 'press|longPress',
      default: 'longPress',
      platform: 'android/ios',
      description:
        'work with native prop and will take press and longPress. by default it is longPress for ContextMenu',
    },
  ]}
/>

### Content

Contains the content of the Context Menu

<PropsTable
  data={[
    {
      name: 'children',
      type: 'React.ReactNode',
      required: true,
    },
  ]}
/>

### Item

Context Menu Item

<PropsTable
  data={[
    {
      name: 'key',
      type: 'React.ReactNode',
      required: true,
    },

    {
      name: 'disabled',
      type: 'boolean',
      required: false,
      default: false
    },
    {
      name: 'destructive',
      type: 'boolean',
      required: false,
    },
    {
      name: 'hidden',
      type: 'boolean',
      required: false
    },
    {
      name: 'onSelect',
      type: 'function',
      required: true
    },
    {
      name: 'onFocus',
      type: 'function',
      required: false,
    },
    {
      name: 'onBlur',
      type: 'function',
      required: false,
    },
    {
      name: 'textValue',
      type: 'string',
      required: false,
      platform: 'ios/android',
      description: 'when content of ItemTitle is not a string then this value is necessary'
    }

]}
/>

### ItemTitle

Context Menu ItemTitle to render Title of the Menu

<PropsTable
  data={[
    {
      name: 'children',
      type: 'React.ReactNode',
      required: true,
    },
  ]}
/>
<Notice>
you can directly pass a text node to the ItemTitle.
how ever if you use a nested React node like `<Text>` then you need to pass textValue to the `<Item>`
so that it works with the native menus
</Notice>

### ItemIcon

Component to render Icon, for non native Menu you can pass an Icon component
for native Menus you can pass platform specific icons to the `android`/`ios` props

on iOS it renders platform [SF Symbol](https://hotpot.ai/free-icons?s=sfSymbols) icons

<PropsTable
  data={[
    {
      name: 'ios',
      type: 'object',
      required: false,
    },
    {
      name: 'android',
      type: 'object',
      required: false,
    },
  ]}
/>

```jsx line=1
<ContextMenu.ItemIcon
  ios={{
    name: '0.circle.fill', // required
    pointSize: 5,
    weight: 'semibold',
    scale: 'medium',
    // can also be a color string. Requires iOS 15+
    hierarchicalColor: {
      dark: 'blue',
      light: 'green',
    },
    // alternative to hierarchical color. Requires iOS 15+
    paletteColors: [
      {
        dark: 'blue',
        light: 'green',
      },
    ],
  }}
>
  <CircleIcon />
</ContextMenu.ItemIcon>
```

### ItemImage

Component Render an Item Image, for the native Menus it only works on ios
it takes same properties as @tamagui/image

### ItemSubtitle

Component Render a subtitle for the Menu Item
for the native Menus ot only works on ios

<PropsTable
  data={[
    {
      name: 'children',
      type: 'React.ReactNode',
      required: true,
    },
  ]}
/>

### Group

Component that Group multiple Items

### CheckboxItem

Usage is similar to Item with added checkbox features.

<PropsTable
  data={[
    {
      name: 'key',
      type: 'React.ReactNode',
      required: true,
    },
    {
      name: 'disabled',
      type: 'boolean',
      required: false,
      default: false,
    },
    {
      name: 'destructive',
      type: 'boolean',
      required: false,
    },
    {
      name: 'hidden',
      type: 'boolean',
      required: false,
    },
    {
      name: 'onFocus',
      type: 'function',
      required: false,
    },
    {
      name: 'onBlur',
      type: 'function',
      required: false,
    },
    {
      name: 'textValue',
      type: 'string',
      required: false,
      platform: 'ios/android',
      description:
        'when content of ItemTitle is not a string then this value is necessary',
    },
    {
      name: 'value',
      type: 'on|off|mixed',
      required: true,
      platform: 'ios|android',
    },
    {
      name: 'onValueChange',
      type: 'function',
      required: true,
      platform: 'ios|android',
    },
    {
      name: 'checked',
      type: 'boolean',
      required: true,
      description: 'this value is necessary for non native Menus',
    },
    {
      name: 'onCheckedChange',
      type: 'function',
      required: true,
      description: 'this value is necessary for non native Menus',
    },
  ]}
/>

### ItemIndicator

Use inside CheckboxItem or RadioItem to render when item is checked, so basically conditionally let you render a check mark

```jsx line=1
<ContextMenu.ItemIndicator>
  <CheckmarkIcon /> {/* doesn't work with native prop */}
</ContextMenu.ItemIndicator>
```

### Lable

Render and non focusable Label

on native Menus only one Label is supported per each Menu and SubMenu

### Arrow

Render an Arrow componen

on native Menus only one Label is supported per each Menu and SubMenu

<PropsTable
  data={[
    {
      name: 'size',
      type: 'number|SizeToken',
      required: true,
    },
  ]}
/>

### Separator

Render a content Separator component
only work with non Native Menus

### Sub

Component hold Sub Menu part

### SubContent

Render Content of SubMenu

### SubTrigger

same as Trigger but for SubMenu

### Preview (iOS only)

When Context Menu is Visible render a Custom Component
needs to pass a function as a child
only works with native menu iOS

<Notice>
  Should be rendered as a child of `<Item.Content />`
</Notice>

<PropsTable
  data={[
    {
      name: 'size',
      type: 'number',
      platform: 'ios',
    },
    {
      name: 'onPress',
      type: 'function',
      platform: 'ios',
    },
    {
      name: 'isResizeAnimated',
      type: 'boolean',
      default: true,
      platform: 'ios',
    },
    {
      name: 'borderRadius',
      type: 'number',
      required: false,
    },
    {
      name: 'backgroundColor',
      type: 'object',
    },
    {
      name: 'preferredCommitStyle',
      type: 'pop|dismiss',
    },
  ]}
/>

```jsx line=1
<ContextMenu.Preview
  // optional props:
  preferredCommitStyle="pop" // or "dismiss"
  backgroundColor={{
    // or a color string directly
    dark: 'black',
    light: 'white',
  }}
>
  {() => <Preview />}
</ContextMenu.Preview>
```
