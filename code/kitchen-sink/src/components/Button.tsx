// The canonical v2-look Button skin is the default styled Button in `tamagui`
// (the unstyled @tamagui/ui primitive + the skin layered in tamagui). Kitchen-
// sink consumes it directly — no duplicated skin here.
export {
  Button,
  ButtonFrame,
  ButtonIcon,
  ButtonText,
  buttonSizes,
  type ButtonProps,
  type ButtonSize,
} from 'tamagui'
