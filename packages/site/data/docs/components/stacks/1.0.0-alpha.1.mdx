---
title: Stacks
description: Stacks are you lowest level view for laying things out.
name: stacks
---

# Stacks

<Description>
  The core view element in Tamagui for creating flex-based vertical and horizontal&nbsp;layouts.
</Description>

<HeroContainer>
  <StacksDemo />
</HeroContainer>

```jsx hero template=Stacks

```

<Highlights
  features={[
    'Vertical and horizontal stacks that optimize.',
    'Spacing property to add space between elements.',
    'Supports all the same properties as React Native <View />.',
  ]}
/>

## Usage

XStack and YStack are the base views in Tamagui, they both extend directly off the simple [Stack](/docs/core/stack-and-text) view from `@tamagui/core`. They accept [every prop from react-native-web](https://necolas.github.io/react-native-web/docs/view/) `<View />`, as well as all [the style properties Tamagui supports](/docs/intro/props).

In this example you'd show three `YStack` elements spaced out.

```jsx
import { YStack, XStack } from 'tamagui'

export default () => (
  <XStack spacing>
    <YStack />
    <YStack />
    <YStack />
  </XStack>
);
```

To see all the style properties supported, see the [Props](/docs/intro/props) documentation.

## Fuller example

An example using a wide variety of style properties:

```jsx
import { XStack, YStack, Text } from 'tamagui'

export default () => (
  <XStack
    flex={1}
    flexWrap="wrap"
    backgroundColor="#fff"
    hoverStyle={{
      backgroundColor: 'red'
    }}
    // media query
    $gtSm={{
      flexDirection: 'column',
      flexWrap: 'nowrap'
    }}
  >
    <YStack spacing="$3">
      <Text>Hello</Text>
      <Text>World</Text>
    </YStack>
  </XStack>
)
```
