{
  "name": "@tamagui/shapes",
  "version": "1.0.1-rc.0.3",
  "sideEffects": [
    "*.css"
  ],
  "source": "src/index.ts",
  "types": "./types/index.d.ts",
  "main": "dist/cjs",
  "module": "dist/esm",
  "module:jsx": "dist/jsx",
  "files": [
    "src",
    "types",
    "dist"
  ],
  "scripts": {
    "build": "tamagui-build",
    "watch": "tamagui-build --watch",
    "lint": "eslint src",
    "lint:fix": "yarn lint --fix",
    "clean": "tamagui-build clean",
    "clean:build": "tamagui-build clean:build"
  },
  "dependencies": {
    "@tamagui/core": "^1.0.1-rc.0.3",
    "@tamagui/stacks": "^1.0.1-rc.0.3"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@tamagui/build": "^1.0.1-rc.0.3",
    "react": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
