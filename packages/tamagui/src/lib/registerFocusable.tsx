export const registerFocusable = (id: string, input: { focus: Function }) => {
  // noop focus is handed natively
}

export const unregisterFocusable = (id: string) => {
  // noop focus is handed natively
}

export const focusFocusable = (id: string) => {
  // noop focus is handed natively
}
