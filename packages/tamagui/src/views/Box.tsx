import { styled } from '@tamagui/core'

import { YStack } from './Stacks'

// TODO maybe not necessary here, could be card instead

export const Box = styled(YStack, {
  backgroundColor: '$bg2',
  elevation: '$1',
})
