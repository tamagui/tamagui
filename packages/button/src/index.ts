// entry point for tamagui's default (styled) button
export {
  Button,
  ButtonFrame,
  ButtonText,
  createButtonScope,
  useButton,
  buttonStaticConfig,
} from './themed'
export type { ButtonProps } from './themed'
