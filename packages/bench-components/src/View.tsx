import { Stack, styled } from '@tamagui/core'

export const View = styled(Stack, {
  alignItems: 'stretch',
  display: 'flex',
  flexBasis: 'auto',
  flexDirection: 'column',
  flexShrink: 0,
  position: 'relative',
  minHeight: '0',
  minWidth: '0',
})
