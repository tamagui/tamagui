{
  "name": "@tamagui/config-default",
  "version": "1.0.20",
  "source": "src/index.ts",
  "main": "dist",
  "types": "./types/index.d.ts",
  "scripts": {
    "build": "tamagui-build",
    "watch": "tamagui-build --watch",
    "clean": "tamagui-build clean",
    "clean:build": "tamagui-build clean:build"
  },
  "files": [
    "types",
    "dist"
  ],
  "dependencies": {
    "@tamagui/core": "^1.0.20",
    "@tamagui/shorthands": "^1.0.20"
  },
  "devDependencies": {
    "@tamagui/build": "^1.0.20"
  },
  "publishConfig": {
    "access": "public"
  }
}
