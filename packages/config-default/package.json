{
  "name": "@tamagui/config-default",
  "version": "1.100.0",
  "source": "src/index.ts",
  "main": "dist",
  "types": "./types/index.d.ts",
  "scripts": {
    "build": "tamagui-build",
    "watch": "tamagui-build --watch",
    "clean": "tamagui-build clean",
    "clean:build": "tamagui-build clean:build"
  },
  "files": [
    "types",
    "dist"
  ],
  "dependencies": {
    "@tamagui/animations-css": "1.100.0",
    "@tamagui/core": "1.100.0",
    "@tamagui/shorthands": "1.100.0"
  },
  "devDependencies": {
    "@tamagui/build": "1.100.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
