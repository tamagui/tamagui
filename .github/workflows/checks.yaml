name: Checks

on:
  pull_request:
  push:
    paths-ignore:
      - 'assets/**'
      - '.vscode/**'
    branches:
      - main
      - v2
      - 'v2-*'

# cancel in-progress runs on the same branch (avoids duplicate PR + push runs)
concurrency:
  group: checks-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install
        uses: ./.github/actions/install

      - name: Check
        run: bun run check

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint

  unit-tests:
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: '--max-old-space-size=6144'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install
        uses: ./.github/actions/install

      - name: Install playwright
        run: bunx playwright install

      - name: Run unit tests
        run: bun turbo run test:web --filter='!@tamagui/kitchen-sink' --concurrency=1

  integration-tests:
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: '--max-old-space-size=6144'
    strategy:
      fail-fast: false
      matrix:
        shard: [1/4, 2/4, 3/4, 4/4]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install
        uses: ./.github/actions/install

      - name: Install playwright
        run: bunx playwright install --with-deps

      - uses: nick-fields/retry@v3
        name: 'Run integration tests (shard ${{ matrix.shard }})'
        with:
          timeout_minutes: 15
          max_attempts: 2
          command: cd code/kitchen-sink && NODE_ENV=test node -r esbuild-register ../../node_modules/.bin/playwright test --shard=${{ matrix.shard }}
