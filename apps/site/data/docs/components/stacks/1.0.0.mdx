---
title: Stacks
description: Stacks are your lowest level view for laying things out
name: stacks
component: Stacks
package: stacks
demoName: Stacks
---

# Stacks

<Description>
  An optional base for creating flex-based&nbsp;layouts.
</Description>

<HeroContainer>
  <StacksDemo />
</HeroContainer>

```tsx hero template=Stacks

```

<Highlights
  features={[
    'X, Y, and Z stacks for easy flex layouts.',
    'Gap property to add space between elements.',
    'Handle press, focus, and layout events easily.',
  ]}
/>

## Usage

XStack, YStack and ZStack are the base views in Tamagui, they both extend directly off the base [View](/docs/core/stack-and-text) from `@tamagui/core`.

Stack props accept [every prop from react-native-web](https://necolas.github.io/react-native-web/docs/view/) View, as well as all [the style properties Tamagui supports](/docs/intro/props).

In this example you'd show three `YStack` elements spaced out.

```tsx
import { XStack, YStack } from 'tamagui'

export default () => (
  <XStack gap="$2">
    <YStack />
    <YStack />
    <YStack />
  </XStack>
)
```

To see all the style properties supported, see the [Props](/docs/intro/props) documentation.

### Fuller example

An example using a wide variety of style properties:

```tsx
import { Text, XStack, YStack } from 'tamagui'

export default () => (
  <XStack
    flex={1}
    flexWrap="wrap"
    backgroundColor="#fff"
    hoverStyle={{
      backgroundColor: 'red',
    }}
    // media query
    $gtSm={{
      flexDirection: 'column',
      flexWrap: 'nowrap',
    }}
  >
    <YStack gap="$3">
      <Text>Hello</Text>
      <Text>World</Text>
    </YStack>
  </XStack>
)
```

## API Reference

### Stack

Beyond the [Tamagui Props](/docs/intro/props), stacks have two variants

<PropsTable
  data={[
    {
      name: 'fullscreen',
      required: false,
      type: 'boolean',
      description: (
        <span>Sets position: absolute, top: 0, left: 0, right: 0, bottom: 0.</span>
      ),
    },
    {
      name: 'elevation',
      required: false,
      type: 'number | tokens.size',
      description: (
        <span>
          Sets a natural looking shadow that expands out and away as the size gets bigger.
        </span>
      ),
    },
  ]}
/>

### ThemeableStack

Adds a variety of helpers that automatically apply theme values when set to true. Useful for creating higher level components by wrapping with `styled()`:

```tsx
import { styled } from 'tamagui' // or '@tamagui/core'
import { ThemeableStack } from 'tamagui' // or '@tamagui/stacks'

const MyCard = styled(ThemeableStack, {
  hoverTheme: true,
  pressTheme: true,
  focusTheme: true,
  bordered: true,
})
```

Beyond Stacks props, ThemeableStacks add:

<PropsTable
  data={[
    {
      name: 'hoverTheme',
      required: false,
      default: false,
      type: 'boolean',
      description: `Sets backgroundColor and borderColor to matching theme values on hover.`,
    },
    {
      name: 'focusTheme',
      required: false,
      default: false,
      type: 'boolean',
      description: `Sets backgroundColor and borderColor to matching theme values on focus.`,
    },
    {
      name: 'pressTheme',
      required: false,
      default: false,
      type: 'boolean',
      description: `Sets backgroundColor and borderColor to matching theme values on press.`,
    },
    {
      name: 'pad',
      required: false,
      default: false,
      type: 'boolean',
      description: `Matches padding to size prop, if given.`,
    },
    {
      name: 'elevate',
      required: false,
      default: false,
      type: 'boolean',
      description: `Matches elevation prop to size prop, if given.`,
    },
    {
      name: 'bordered',
      required: false,
      default: false,
      type: 'boolean | number',
      description: `Adds a border that inherits themes.`,
    },
    {
      name: 'circular',
      required: false,
      default: false,
      type: 'boolean | number',
      description: `Clamps to a circular shape and radius.`,
    },
  ]}
/>
